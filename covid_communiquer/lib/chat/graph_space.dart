import 'package:flutter/material.dart';
import 'dart:async';

class GraphSpace extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrangeAccent,
    );
  }
}